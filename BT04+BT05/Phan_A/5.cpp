#include <bits/stdc++.h>
using namespace std;

int main() {
    const int N = 10; 
    char str[N]; 

    cout << "Nhập chuỗi: ";
    cin >> str;

    // In chuỗi ra màn hình (chặn đuôi bằng dấu gạch dưới)
    cout << "Chuỗi đã nhập: " << "_" << str << "_" << endl;

    return 0;
}